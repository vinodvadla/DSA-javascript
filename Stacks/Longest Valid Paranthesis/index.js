var longestValidParentheses = function(s) {
    let stack=[];
    for(let i=0;i<s.length;i++){
        stack.push(s[i])
        if(peek())
        
    }
};

let peek=(s)=>{
    return s[s.length-1]
}